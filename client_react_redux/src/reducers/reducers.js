const initialState = {
    name: '',
    userid: '',
    username: '',
    password: '',
    skills: '',
    contact: '',
    aboutMe: ''
}

const reducers = (state = initialState, action) => {
  switch(action.type) {

    default:
      break;
  }
  return state;
};

export default reducers;
