{
  "name": "freelancer-lab",
  "version": "1.0.0",
  "main": "index.js",
  "repository": {},
  "license": "MIT",
  "dependencies": {
    "concurrently": "^3.5.1"
  },
  "scripts": {
    "build": "concurrently \"cd client_react_redux && yarn build\" \"cd server_express && yarn build\"",
    "install": "(cd client_react_redux && yarn) && (cd server_express && yarn)",
    "start": "concurrently \"cd client_react_redux && set PORT=3000 && yarn start\" \"cd server_express && set PORT=3001 && yarn start\"",
    "start:prod": "cd server_express && yarn start:prod",
    "heroku-postbuild": "yarn build"
  }
}
